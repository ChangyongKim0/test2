import React from "react";
import { HashRouter } from "react-router-dom";
//  BrowserRouter,
import App from "../App";

const Root = () => (
  <HashRouter>
    <App />
  </HashRouter>
);

export default Root;
